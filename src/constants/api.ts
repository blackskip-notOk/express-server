export const API_BASE_URL = "/api"

const PING = "ping"
const SIGNUP = "signup"
const LOGIN = "login"
const AUTH = "auth"

export const API_PING = `${API_BASE_URL}/${PING}`
export const API_SINGUP = `${API_BASE_URL}/${SIGNUP}`
export const API_LOGIN = `${API_BASE_URL}/${LOGIN}`
export const APP_AUTH = `${API_BASE_URL}/${AUTH}`
